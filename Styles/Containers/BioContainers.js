import { StyleSheet } from "react-native";

const BioContainers = StyleSheet.create({
  title: {
    flex: 1,
    backgroundColor: "bisque",

    margin: 10,
  },
  body: {
    flex: 10,
    flexDirection: "column",
    backgroundColor: "aliceblue",
  },
});

export default BioContainers;
